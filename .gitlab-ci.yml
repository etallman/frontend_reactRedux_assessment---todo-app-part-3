image: node:alpine
pages:
  stage: deploy
  script:
  - npm install
  - npm run build
  - rm -rf public
  - mv build public
  variables:
    PUBLIC_URL: "/todo-part3/"
  artifacts:
    paths:
    - public
  only:
  - master